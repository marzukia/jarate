# docs/ — {{PROJECT}}

Pointer index. Follow the links rather than guessing; every link must
resolve. A 404 here means the index is stale — fix it in the same PR as
the change.

- [`DEVELOPMENT.md`](DEVELOPMENT.md) — cold-clone setup and test
  commands. The setup source of truth.
- [`SECRETS.md`](SECRETS.md) — secrets inventory. Must stay in sync with
  `.env.example` (one row per var, both ways).

Add a doc per durable topic (architecture, deployment, operations).
Short and current beats long and stale.
