# Development — {{PROJECT}}

Cold-clone walkthrough: prerequisites, setup, test commands. This file
is the setup source of truth — if it doesn't work on a fresh clone, the
doc is wrong; fix the doc in the same PR.

## Prerequisites

<!-- tools + versions, host-level system libs (the ones a package
     manager cannot install), database bring-up -->

## Setup

<!-- step by step from a fresh clone to a running dev environment -->

## Test commands

<!-- the exact commands `make preflight` runs, plus any heavier
     integration/e2e paths and when to use them -->
