# {{REPO}} — env template
#
# Copy to .env (gitignored) and adjust:  cp .env.example .env
#
# Rule (see AGENTS.md §4): every variable read by the code has a row
# here and in docs/SECRETS.md. Dev-safe defaults; secrets stay out of
# the repo.

# --- app ---
# SECRET_KEY=
# DEBUG=true

# --- database ---
# DATABASE_URL=postgresql://user:pass@localhost:5432/{{REPO}}
