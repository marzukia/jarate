#!/usr/bin/env node
import { runCoordinatorProcess } from "../experimental/coordinator.js";
import { consumeInternalProcessRole, getInternalProcessRole } from "../experimental/process.js";
import { restoreSandboxEnv } from "./restore-sandbox-env.js";
restoreSandboxEnv();
if (getInternalProcessRole() === "coordinator") {
    consumeInternalProcessRole();
    await runCoordinatorProcess(process.argv.slice(2));
}
else {
    await import("./runtime-setup.js");
    await import("../cli.js");
}
//# sourceMappingURL=cli.js.map