{
  "name": "jarate",
  "private": true,
  "description": "Monorepo for the pi + bridge agent stack: @jarate/bridge, pi-bg dispatch, agent-say, installer.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "bunx biome check .",
    "test": "cd packages/bridge && bun x bun test",
    "typecheck": "cd packages/bridge && bun x tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12"
  }
}
